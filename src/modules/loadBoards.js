import Help from "../helper";

// Load project boards into HTML content
const InnitBoardView = (()=> {


    const loadBoards = () => {

        // Will later check for local storage items
        const checkBoards = (boardArr) => 1
    }

    return {
        loadBoards
    }
})()


export default InnitBoardView